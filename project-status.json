{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M2",
  "activeMilestoneTitle": "Adaptive Baseline: Max Pressure",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "active",
    "M3": "locked",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-20",
    "summary": "M2.1 done. Added a Controller interface (observe()/decide() emitting HOLD/SWITCH) in src/controllers/Controller.ts, a FixedTimeController class implementing it, and the environment-enforced signal-safety machine src/simulation/signalMachine.ts (applySignalIntent: min-green + yellow; buildObservationInput seam). Recorded D-008. stepFixedSignal and TrafficEngine untouched, so the M1 golden baseline is byte-identical; a Fixed-vs-legacy equivalence test locks the interface to legacy behaviour. npm run check passes (98 tests + build). MaxPressure decision and pressure computation remain unimplemented (M2.2/M2.3).",
    "nextActions": [
      "M2.2: implement lane pressure computation (upstream demand minus downstream capacity) with hand fixtures; extend the observation (approach queue/pressure by axis); do not tune signals yet.",
      "M2.3/M2.4: implement MaxPressureController.decide with deterministic tie-break, driven through applySignalIntent for environment-enforced min-green + yellow (agent emits HOLD/SWITCH intent only).",
      "M2.3: rewire TrafficEngine to drive a Controller (Fixed via FixedTimeController) and guard with goldenRun.test.ts fixture equality; if it drifts, revert the engine wiring and keep the interface.",
      "M2.5/M2.6: save Fixed vs MaxPressure smoke results on balanced and rush scenarios; record outcomes as-is without assuming MaxPressure wins (R2, D-006).",
      "Record any reward/decision-semantics choice in docs/DECISIONS.md before coding it (D-006 common-random-numbers still applies; D-008 controller contract now in effect)."
    ]
  }
}
