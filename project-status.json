{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M4.1 shared-DQN observation encoder/normalizer (NO training code). Phase C: D-015 in docs/DECISIONS.md + 'Observation encoding — M4' in docs/DATA_CONTRACTS.md. src/rl/observation.ts: encodeObservation(input) -> length-4 vector [tanh(pressureCurrent/S), tanh(pressureOther/S), clamp01(phaseElapsedSec/T), minGreenSatisfied?1:0]; current/other framing is relative to the green axis (activeAxis) so the shared net is phase-symmetric (D-002); consumes the existing ObservationInput (NO new engine seam); scales derived from single-source constants (PRESSURE_OBS_SCALE=EDGE_CAPACITY=16, PHASE_TIME_OBS_SCALE=FIXED_GREEN_SEC=20, no hardcoding); green-only (YELLOW/activeAxis null throws). src/rl/observation.test.ts (13): feature order/length, constant derivation, zero vector, current/other mapping, phase symmetry, sign, tanh scale ref, [-1,1] bounded+finite at extremes, phaseProgress [0,1] clamp, minGreen 0/1, all-finite, purity/no-mutation, YELLOW throw. src/rl/README.md updated to M4 active. npm run check PASS (28 files, 195 tests, build ok). golden + m2Comparison still pass; summary()/metricVersion/simulation unchanged. Milestone/gates UNCHANGED (M4 still active) — M4.1 is a foundation slice, not an exit criterion. DQN/TFJS/replay/target/worker/model/eval NOT implemented (later M4 slices, locked).",
    "nextActions": [
      "M4.2 action adapter: map HOLD|SWITCH through the existing Controller contract (src/rl/ or controllers/); keep min-green/yellow environment-owned via signalMachine.applySignalIntent — agent never sets colour/yellow; safety tests stay environment-owned. Policy injectable seam, still no training.",
      "M4.3 replay buffer (pure, deterministic sampling test) -> M4.4 online+target network (shape test) -> M4.5 epsilon schedule (deterministic) -> M4.6 DQN update step (loss finite, tensors disposed).",
      "M4.7 training worker protocol (message integration test, ARCHITECTURE main<->worker boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner (train/eval seed separation, TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (do not hide, R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
