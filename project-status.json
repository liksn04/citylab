{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M4.2 shared-DQN action adapter + injectable policy seam (NO training code). Phase C: D-016 in docs/DECISIONS.md + 'Action space — M4' in docs/DATA_CONTRACTS.md. src/rl/action.ts: ACTIONS=['HOLD','SWITCH'] (index 0/1), ACTION_SIZE=2, actionToIntent (range/integer-checked, throws), intentToAction. src/rl/DqnController.ts: Policy=(number[])=>number injectable seam + DqnController implements Controller<ObservationInput>; observe passes full input, decide guards yellow->HOLD then encodeObservation (M4.1) -> policy -> actionToIntent; NO tensors/training (policy injected); id defaults dqn-v1. Safety stays environment-owned (applySignalIntent): an always-SWITCH policy still cannot switch before min-green or skip yellow. src/rl/action.test.ts (4) + src/rl/DqnController.test.ts (7, incl. env-owned safety integration). NOT wired into TrafficEngine.controllerKind and no provenance/runConfig change (deferred to M4.9/M4.10 where a real policy exists). npm run check PASS (30 files, 206 tests, build ok). golden/m2/summary()/metricVersion/simulation unchanged. Milestone/gates UNCHANGED (M4 still active) — M4.2 is a foundation slice, not an exit criterion. Prior M4.1 (observation encoder, D-015) also done.",
    "nextActions": [
      "M4.3 replay buffer (src/rl/): pure ring buffer (capacity, push(transition), sample(batchSize, rng)) with a deterministic seeded-sampling test. Transition = {obs:number[], action:number, reward:number, nextObs:number[], done:boolean}. The REWARD definition (wait/throughput-based) is an experiment metric / decision-semantics -> record in docs/DECISIONS.md BEFORE coding (ADR required for reward per AI_START_HERE core constraints).",
      "M4.4 online + target network (TensorFlow.js): input OBSERVATION_SIZE -> hidden -> ACTION_SIZE Q-head, target clone; shape test + tensor dispose (no leak). M4.5 epsilon schedule (deterministic). M4.6 DQN update step (loss finite, tensors disposed).",
      "M4.7 training worker protocol (message integration test, ARCHITECTURE main<->worker boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
