{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M5",
  "activeMilestoneTitle": "Analytics & Neural Inspection",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "done",
    "M5": "active",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true,
    "M4_nonBlockingTrainingBoundary": true,
    "M4_tensorLeakChecked": true,
    "M4_fixedEvaluationSeed": true,
    "M4_trainEvalSeedSeparation": true,
    "M4_modelSnapshotSaveLoad": true,
    "M4_fixedBaselineImprovement": true,
    "M4_failureRecordPreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4 CLOSEOUT + M5 ACTIVATION. Re-verified all 7 M4 exit criteria against code+tests, then advanced M4->done / M5->active (AI_AGENT_GUIDE Milestone advancement protocol). Evidence: (1) non-blocking training boundary — worker protocol (trainingProtocol.ts pure TrainingSession + trainingWorker.ts glue), chunked train + progress/loss, leak 0; trainDqn/evaluateDqn are HEADLESS utils, NOT wired into a React training UI or a live worker yet. (2) tensor leak — tf.memory().numTensors flat across qNetwork/dqnUpdate/modelStorage/trainingProtocol/dqnTraining tests. (3) fixed eval seed — evaluateDqn greedy(eps=0) determinism (toEqual). (4) train/eval seed separation — trainDqn(trainSeeds) vs evaluateDqnVsFixed(evalSeeds); dqnBaseline.test asserts disjoint; measured train 41021.. / eval 51001... (5) model snapshot — modelStorage.ts tf.io ModelArtifacts serialize/load with bit-identical prediction parity (NOT an IndexedDB/user-facing model-library workflow). (6) Fixed improvement — balanced-4x4-v1 measured clean win across 4 disjoint eval seeds (avg 2.64 vs 12.20, p95 9.5 vs 37.5, throughput 591 vs 588, maxQueue 2.25 vs 4.25, switches 341 vs 1248); committed tests do NOT assert a winner (stochastic init, R8). (7) failure preserved — prior gamma-0.95 MIXED result (throughput -32%, over-switching) kept in PROGRESS as R2 evidence. API cleanup: runExperiment/SeedRun/ExperimentResult now typed to BaselineControllerKind = Exclude<ControllerKind,'dqn'> (fixed|maxpressure), so the baseline seed-set runner no longer advertises an unrunnable 'dqn' comparison ('dqn' needs an injected trained model, D-021); tsc -b enforces the @ts-expect-error regression in runExperiment.test. App.tsx stale 'M4 active - training not built yet' -> neutral 'Live simulation - Fixed baseline'. README updated to M0-M4 done / M5 active. UNCHANGED: reward D-017, observation 4-vector + HOLD|SWITCH action contracts, gamma 0.99, DQN_HIDDEN_UNITS 64, learning rate, golden Fixed / m2 fixtures, METRIC_VERSION, tick/queue/wait semantics, traffic physics, MaxPressure. No M5 feature implemented (analytics UI is the newly-active milestone).",
    "nextActions": [
      "M5.1 comparison data model: express Fixed / MaxPressure / trained-DQN results in ONE comparison read model, without recomputing metrics in chart code. NOTE: DQN evaluation needs a trained model (injected controller), so do NOT force it into the baseline runExperiment()/BaselineControllerKind path — design a separate contract that carries the trained-model comparison (and DQN provenance) alongside the baseline runs.",
      "M5.2 avg-waiting-time / throughput visualization from actual raw/sample/run data only (no hardcoded numbers), same scale for comparison.",
      "M5.3 P95 / max-queue / starvation view so R2 (avg improves but tail/starvation) is visible.",
      "M5.4 congestion heatmap from engine-derived data only.",
      "M5.5 run provenance inspector: scenario, scenarioVersion, controller, seed, configHash, metricVersion, train/eval seed separation. If DQN model/training provenance is needed, design it in M5.1 — do NOT stuff model weights into the existing configHash (non-deterministic/backend-dependent, R8/D-021).",
      "M5.6 neural inspection: show observation input / activation / Q output only — no 'explains why the AI decided' framing (charter non-goal).",
      "M5.7 keyboard/touch chart inspection path; plus M5 a11y exit criteria (reduced motion / reduced transparency fallback).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
