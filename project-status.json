{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4.3 replay buffer + reward definition (NO training code). User chose the reward direction: negative waiting/queue. Phase C: D-017 in docs/DECISIONS.md + 'Reward — M4' and 'Replay transition — M4' in docs/DATA_CONTRACTS.md. src/rl/reward.ts: per-intersection local reward = -sum of Q2 approach queues entering the intersection (queueReward + computeQueueRewards, mirrors computePressure; approach edges only, excludes continuations; -0 normalized to 0; always <= 0). src/rl/replayBuffer.ts: Transition = {obs:number[], action:number, reward:number, nextObs:number[], done:boolean}; ReplayBuffer fixed-capacity ring (push overwrites oldest, sample(batchSize, rng: RandomSource) uniform WITH replacement via project seeded RNG -> deterministic; throws on empty/non-positive batch; capacity validated). NO tensors/training. src/rl/reward.test.ts (7) + src/rl/replayBuffer.test.ts (6, incl. ring eviction, determinism). npm run check PASS (32 files, 219 tests, build ok). golden/m2/summary()/metricVersion/simulation unchanged; engine run path NOT wired. Milestone/gates UNCHANGED (M4 still active) — M4.3 is a foundation slice, not an exit criterion. Prior M4.1 (obs encoder D-015) and M4.2 (action adapter D-016) also done.",
    "nextActions": [
      "M4.4 online + target network (TensorFlow.js, src/rl/): input OBSERVATION_SIZE -> hidden -> ACTION_SIZE Q-head; target is a clone of online, synced periodically. shape test + tensor dispose / no leak (compare tf.memory().numTensors before/after). Record network hyperparameters (hidden size, learning rate) briefly in docs/DECISIONS.md.",
      "M4.5 epsilon-greedy schedule (pure, deterministic): start/end/decay -> step->epsilon, schedule test. M4.6 DQN update step: sample replay -> target y = r + gamma*maxQ(next)*(1-done), Huber/MSE loss, tensors disposed, loss finite. reward scaling / gamma live here (separate from D-017 definition).",
      "M4.7 training worker protocol (main<->worker messages, ARCHITECTURE boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
