{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4.9 DQN engine wiring + evaluation/training runner (D-021). TrafficEngine: ControllerKind += 'dqn' (requires EngineConfig.injectedController — engine never imports rl), read-only onDecisionTick hook emitting green decisions {intersectionId, observation, intent} + queueByEdge; hook omitted => byte-identical (Fixed golden preserved). runConfig: dqn canonicalized as adaptive (greenSec null, minGreenSec MIN_GREEN); provenance CONTROLLER_IDS.dqn='dqn-v1'. src/rl/transitionBuilder.ts: pure TransitionCollector = decision-point transitions with reward accumulated between an intersection's consecutive green decisions. src/rl/dqnPolicy.ts: greedyPolicy + epsilonGreedyPolicy. src/rl/dqnTraining.ts: evaluateDqn(model, scenario) (deterministic greedy engine run -> RunSummary) + trainDqn(config) (episode rollout via hook -> replay learning, target sync, epsilon decay, train seed set). Tests +17 (transitionBuilder 4, dqnPolicy 3, dqnTraining 5, dqnWiring 6; provenance expectation updated for dqn). npm run check PASS (41 files, 273 tests, build ok); golden Fixed byte-identical. Milestone/gates UNCHANGED (M4 active). Exit criteria met so far: non-blocking (worker M4.7), tensor-leak (whole pipeline leak-0), model snapshot (M4.8), eval seed fixed + train/eval seed separation. REMAINING: demonstrate improvement vs Fixed over repeated eval + record failures (M4.10). Prior M4.1-M4.8 done.",
    "nextActions": [
      "M4.10: run trainDqn, then evaluate the trained model with evaluateDqn across an EVAL seed set (disjoint from train seeds) and compare to the Fixed baseline (runScenario 'fixed') on avg/p95 wait, throughput, maxQueue — from ACTUAL runs, recorded as measured (improvement, regression, or failure all kept, R2). Note training is slow (per-intersection single-obs predict), so size the run realistically; do NOT hardcode numbers.",
      "Then re-evaluate the 7 M4 exit criteria. Advance to M4 done / M5 active only if ALL are met (AI_AGENT_GUIDE); if 'improvement vs Fixed' is not achieved, keep M4 active and record the gap honestly (do not fake a pass).",
      "M4.7 training worker protocol (main<->worker messages, ARCHITECTURE boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
