{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M3",
  "activeMilestoneTitle": "Experiment Runner & Data Provenance",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "active",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M3.1-M3.4 done (M3 still active). M3.1/M3.2: D-010; scenarioVersion:'v1'; runConfig.ts (canonicalize + stableStringify + hashRunConfig 'm3-'+16hex); provenance.ts (RunProvenance + buildRunProvenance); m3-provenance-v1.json. M3.3: runExperiment.ts seed-set runner (both controllers share demand per seed, D-006; provenance attached). M3.4: D-011 first; raw metric-sample time series separated from aggregate RunSummary — analytics/metricSamples.ts (MetricSample), read-only TrafficEngine.sample() (no tick/summary change), sampledRun.ts (runScenarioSampled, cadence must be TICK_SEC multiple), DEFAULT_SAMPLE_INTERVAL_SEC=30, optional sampling in runExperiment (default omits samples => M3.3 shape). Sample maxQueue is instantaneous; RunSummary.maxQueue >= max(sample.maxQueue). npm run check PASS (23 files, 155 tests = 122+17+7+9, build ok). goldenRun + m2Comparison pass AND runScenarioSampled().summary == runScenario() => aggregates byte-identical. M3.5-M3.7 pending.",
    "nextActions": [
      "M3.5: persistence in src/persistence/ mapping ExperimentResult (provenance + summary + samples) into Dexie (reload after refresh); route through a run coordinator, simulation must not call Dexie (ARCHITECTURE). Map to RunRecord/MetricSampleRecord (db.ts skeleton). Assign runtime provenance fields (runId/startedAt/codeVersion) here (D-010). Check whether tests need fake-indexeddb.",
      "M3.6: CSV export (spreadsheet-readable): sample time series + summary/provenance header.",
      "M3.7: JSON export/import roundtrip with metadata; lock the re-analyzable export schema (sample/summary already pass in-memory JSON roundtrip).",
      "Doc hygiene (out of milestone scope): sync docs/MILESTONES.md status labels (M1 ACTIVE/M2 LOCKED/M3 LOCKED) and the UI 'M1 active' copy with project-status.json (M3 active)."
    ]
  }
}
