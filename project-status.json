{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M2",
  "activeMilestoneTitle": "Adaptive Baseline: Max Pressure",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "active",
    "M3": "locked",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-20",
    "summary": "M2.2 done. Added src/simulation/pressure.ts: buildApproachIndex (per-intersection approach edges + same-heading straight continuation, null at boundary), pressureByAxis (axis pressure = sum over approaches of upstream queue minus downstream continuation queue, D-009), computePressure (all intersections). Pure topology + queue arithmetic reusing the M1 Q2 queue definition; no vehicle/analytics deps, no signal tuning. Recorded D-009 and a DATA_CONTRACTS Control-signals note. Hand-fixture tests verify interior/boundary/negative pressure. npm run check passes (107 tests + build). MaxPressure decision and engine rewiring remain (M2.3).",
    "nextActions": [
      "M2.3: implement src/controllers/MaxPressureController.ts (implements Controller); observe() builds an observation carrying axis pressure (AxisPressure), decide() emits SWITCH only when the opposite axis pressure is strictly greater than the current axis (HOLD on tie/lower; deterministic HOLD-first tie-break); min-green + yellow enforced by applySignalIntent.",
      "M2.3: rewire TrafficEngine to drive a Controller each tick (queueLengthsByEdge -> computePressure -> buildObservationInput + pressure -> controller.decide -> applySignalIntent); Fixed via FixedTimeController; guard with goldenRun.test.ts fixture equality (revert wiring if it drifts).",
      "M2.4: extend the min-green contract test at the engine level (an illegal early switch is ignored).",
      "M2.5/M2.6: save Fixed vs MaxPressure summaries on balanced and rush scenarios; record outcomes as-is without assuming MaxPressure wins (R2, D-006).",
      "Record any reward/decision-semantics choice in docs/DECISIONS.md before coding it (D-006 common-random-numbers; D-008 controller contract; D-009 pressure definition now in effect)."
    ]
  }
}
