{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M3",
  "activeMilestoneTitle": "Experiment Runner & Data Provenance",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "active",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M3.1/M3.2 provenance foundation (M3 still active). Recorded D-010 first (Phase C): RunConfig canonical field set = DATA_CONTRACTS Q5 reproducibility inputs, stable key-sorted JSON + pure 2-lane hash, RunProvenance as a SEPARATE wrapper (summary()/METRIC_VERSION/golden+m2 fixtures untouched), runtime fields (runId/startedAt/commit) deferred to persistence (M3.5). Added scenarioVersion:'v1' to scenarios (id unchanged); src/simulation/runConfig.ts (canonicalizeRunConfig + stableStringify + hashRunConfig -> 'm3-'+16hex, same config -> same hash, key-order independent); src/simulation/provenance.ts (RunProvenance + CONTROLLER_IDS + buildRunProvenance); src/simulation/__fixtures__/m3-provenance-v1.json from real derivation (4 distinct hashes). npm run check PASS (21 files, 139 tests = 122+17, build ok). goldenRun + m2Comparison 9/9 PASS => M2 artifacts byte-identical. M3.3-M3.7 not implemented this session.",
    "nextActions": [
      "M3.3: seed-set runner sharing identical demand across controllers, attaching buildRunProvenance to each run (new fn e.g. runExperiment; do NOT change compareControllers/ScenarioComparison — m2-comparison fixture locks its shape).",
      "M3.4: metric sample schema separating aggregate RunSummary from raw per-sample records (MetricSampleRecord); design a periodic sample-extraction hook without changing summary() shape.",
      "M3.5: persistence in src/persistence/ mapping RunProvenance + summary + samples into Dexie (reload after refresh); route through a run coordinator, simulation must not call Dexie (ARCHITECTURE). Assign runtime provenance fields (runId/startedAt/codeVersion) here (D-010).",
      "M3.6/M3.7: CSV (spreadsheet-readable) + JSON export/import roundtrip with metadata.",
      "Doc hygiene (out of milestone scope): sync docs/MILESTONES.md status labels (M1 ACTIVE/M2 LOCKED/M3 LOCKED) and the UI 'M1 active' copy with project-status.json (M3 active)."
    ]
  }
}
