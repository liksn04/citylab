{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M3",
  "activeMilestoneTitle": "Experiment Runner & Data Provenance",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "active",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M3.1-M3.6 done (M3 still active; M3.7 remains). M3.1/M3.2: D-010; scenarioVersion; hashRunConfig; buildRunProvenance. M3.3: runExperiment seed-set runner (D-006). M3.4: D-011; raw MetricSample time series vs aggregate RunSummary; read-only TrafficEngine.sample(). M3.5: D-012; persistence — RunStore port + DexieRunStore + InMemoryRunStore, Dexie version(2), coordinator src/runner/experimentPersistence.ts bridges simulation<->persistence (simulation never imports Dexie), runtime fields via RuntimeMeta; reload-after-refresh proven with fake-indexeddb. M3.6: D-013; src/runner/experimentCsv.ts experimentToCsv -> {runsCsv, samplesCsv} (tidy runs + samples tables, RFC4180 quoting/CRLF, full-precision), golden fixtures m3-runs-v1.csv/m3-samples-v1.csv from real run; PersistedRun gains runId. Build infra: added @types/node@20 devDep + tsconfig.app types:['node','vite/client'] so the fs-based fixture test compiles under the TS7 native compiler (no runtime src change). npm run check PASS (26 files, 173 tests = 165+8, build ok). golden/m2 + M3.1-M3.5 tests all pass; summary()/metricVersion/simulation unchanged.",
    "nextActions": [
      "M3.7: JSON export/import roundtrip with metadata (schema/version tag); export PersistedExperiment/ExperimentBundle to JSON, validate schema+version on import, reconstruct; keep consistent with experimentToRecords/recordsToExperiment; in-memory roundtrip test + golden.",
      "After M3.7: re-evaluate ALL 5 M3 exit criteria; if met, advance project-status to M3 done / M4 active per AI_AGENT_GUIDE Milestone advancement protocol (DQN/TFJS stay locked to M4 — no training code yet).",
      "Wiring run persistence into the app store/UI belongs with M5 (provenance inspector), not M3.",
      "Doc hygiene (out of milestone scope): sync docs/MILESTONES.md status labels (M1 ACTIVE/M2 LOCKED/M3 LOCKED) and the UI 'M1 active' copy with project-status.json (M3 active).",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
