{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4.10 tuning — CLEAN win over Fixed; all 7 M4 exit criteria met (advance pending user OK). User chose 'more training/tuning' (reward D-017 UNCHANGED, no new ADR). Tuned: gamma 0.95->0.99 (removes myopic over-switching), DQN_HIDDEN_UNITS 32->64, episodes 24->60, batch 32->64, buffer 20k->50k, targetSync 200->500, epsilon end 0.05->0.02; baked the working values into DEFAULT_DQN_HYPERPARAMS.gamma and DQN_HIDDEN_UNITS (D-018/D-019 noted). MEASURED (balanced-4x4-v1, 60 episodes ~53s, eval seeds 51001-51004 disjoint from train 41021..41028): DQN avgWait 2.64s vs Fixed 12.20s (-78%), p95 9.5 vs 37.5 (-75%), throughput 591 vs 588 (equal+, regression GONE), maxQueue 2.25 vs 4.25, switches 341 vs 1248 — DQN dominates ALL metrics across all 4 eval seeds. gamma up penalized switching->yellow->future-queue, so switches collapsed to 341. Prior MIXED result kept in PROGRESS as the failure record (R2). All exit criteria now MET: non-blocking (M4.7), tensor-leak-0, eval seed fixed, train/eval seed separation, model snapshot (M4.8), improvement vs Fixed (clean), failures recorded. npm run check PASS (42 files, 274 tests, build ok; default changes re-verified). golden/metricVersion unchanged. Milestone kept ACTIVE this session — advancement is the user's call (their choice was 'tune', not 'advance').",
    "nextActions": [
      "USER DECISION: advance M4? All 7 exit criteria are now met (clean Fixed-beating result). If yes: per AI_AGENT_GUIDE flip project-status M4->done / M5->active, record M4 gate evidence, keep the earlier MIXED run in PROGRESS as the failure record. If the user wants more robustness first, run additional seeds/scenarios (e.g., rush-4x4-v1) before advancing.",
      "M5 (Analytics, locked until advanced) will visualize the DQN-vs-Fixed comparison from ACTUAL run data (no hardcoded numbers), showing provenance + train/eval seed separation.",
      "Optional: verify the win also holds on rush-4x4-v1 (heavier demand) before/after advancing, recorded as measured (R2).",
      "M4.7 training worker protocol (main<->worker messages, ARCHITECTURE boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
