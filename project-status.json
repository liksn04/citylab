{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M3",
  "activeMilestoneTitle": "Experiment Runner & Data Provenance",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "active",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M3.1/M3.2/M3.3 done (M3 still active). M3.1/M3.2: D-010 recorded first; scenarioVersion:'v1' on scenarios; runConfig.ts (canonicalizeRunConfig + stableStringify + hashRunConfig -> 'm3-'+16hex, same config -> same hash, key-order independent); provenance.ts (RunProvenance + CONTROLLER_IDS + buildRunProvenance); m3-provenance-v1.json from real derivation. M3.3: src/simulation/runExperiment.ts (SeedRun/ExperimentResult + runExperiment) runs a scenario across a seed set with both controllers sharing identical demand per seed (D-006) and provenance attached; reuses runScenario + buildRunProvenance, no summary()/golden change. npm run check PASS (22 files, 146 tests = 122+17+7, build ok). goldenRun + m2Comparison still pass => M2 artifacts byte-identical. Note: demand COUNT is rate-driven / seed-independent (demand.ts) — the seed varies the OD draw, so cross-seed variation shows in outcomes (avgWait), not in generated. M3.4-M3.7 pending.",
    "nextActions": [
      "M3.4: metric sample schema separating aggregate RunSummary from raw per-sample time series. Design a non-invasive sampling hook that does NOT change the TrafficEngine tick path or summary() (e.g. the runner collects engine.metrics() + a snapshot maxQueue between tick steps). Consider recording sample cadence in DECISIONS. In-memory roundtrip test.",
      "M3.5: persistence in src/persistence/ mapping ExperimentResult (provenance + summary + samples) into Dexie (reload after refresh); route through a run coordinator, simulation must not call Dexie (ARCHITECTURE). Assign runtime provenance fields (runId/startedAt/codeVersion) here (D-010). Check whether tests need fake-indexeddb.",
      "M3.6/M3.7: CSV (spreadsheet-readable) + JSON export/import roundtrip with metadata.",
      "Doc hygiene (out of milestone scope): sync docs/MILESTONES.md status labels (M1 ACTIVE/M2 LOCKED/M3 LOCKED) and the UI 'M1 active' copy with project-status.json (M3 active)."
    ]
  }
}
