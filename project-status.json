{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M3",
  "activeMilestoneTitle": "Experiment Runner & Data Provenance",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "active",
    "M4": "locked",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-21",
    "summary": "M3.1-M3.5 done (M3 still active). M3.1/M3.2: D-010; scenarioVersion; runConfig.ts hashRunConfig; provenance.ts buildRunProvenance. M3.3: runExperiment.ts seed-set runner (D-006). M3.4: D-011; raw MetricSample time series vs aggregate RunSummary; read-only TrafficEngine.sample(); sampledRun.ts. M3.5: D-012; persistence — src/persistence/db.ts extended (records carry full provenance + verbatim RunSummary, MetricSampleRecord = full MetricSample, Dexie version(2), RunStore port + DexieRunStore + InMemoryRunStore, getRuns sorted by runId); src/runner/experimentPersistence.ts coordinator (experimentToRecords/recordsToExperiment pure + saveExperiment/loadExperiment) bridges simulation<->persistence, assigns runtime fields (runId/startedAt/codeVersion/createdAt via RuntimeMeta); simulation never imports Dexie. Reload-after-refresh proven with fake-indexeddb@6.2.5 (devDep). npm run check PASS (25 files, 165 tests = 155+10, build ok). goldenRun + m2Comparison pass; summary()/metricVersion/simulation unchanged. M3.6/M3.7 pending.",
    "nextActions": [
      "M3.6: CSV export (spreadsheet-readable) from PersistedExperiment/ExperimentResult: sample time series rows + summary/provenance header. Pure function + fixture test.",
      "M3.7: JSON export/import roundtrip with metadata; lock the re-analyzable export schema and validate schema/version on import; keep consistent with experimentToRecords/recordsToExperiment.",
      "Wiring run persistence into the app store/UI belongs with M5 (provenance inspector), not M3.",
      "Doc hygiene (out of milestone scope): sync docs/MILESTONES.md status labels (M1 ACTIVE/M2 LOCKED/M3 LOCKED) and the UI 'M1 active' copy with project-status.json (M3 active).",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
