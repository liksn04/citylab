{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4.7 training worker protocol (pure session + thin glue; NO engine wiring). Phase C: D-020 in docs/DECISIONS.md. src/workers/trainingProtocol.ts: pure TrainingSession owns model/buffer/trainer/epsilon/seeded-RNG and handle(command)->responses; commands init(config)/push(transitions)/train(steps)/stop; responses ready/progress{step,loss,epsilon,bufferSize}/skipped/stopped/error. train runs chunked steps (progress per step), syncs target every targetSyncInterval, skips while buffer<minBufferToTrain; errors before init or on bad steps. src/workers/trainingWorker.ts: thin glue (self cast to DedicatedWorkerGlobalScope; onmessage->handle->postMessage), untested boundary, not app-wired. src/workers/trainingProtocol.test.ts (8, incl. message integration + leak-0). Split (pure session vs glue) so protocol is tested in node without a real Worker. Transitions come via push; running a TrafficEngine inside the worker to generate them is M4.9. npm run check PASS (36 files, 252 tests, build ok incl. worker tsc). golden/m2/summary()/metricVersion unchanged. Milestone/gates UNCHANGED (M4 active). Prior M4.1-M4.6 done.",
    "nextActions": [
      "M4.8 model save/load: tf.io (indexeddb:// or an in-memory IOHandler) to save the online network and reload it; prediction-parity test (same input -> same Q after reload). Decide node test approach (in-memory IOHandler vs fake-indexeddb).",
      "M4.9 eval runner: wire DqnController into TrafficEngine (controllerKind 'dqn', greedy policy = argmax of DqnModel.predictQ) + extend provenance/runConfig (D-010, controllerId 'dqn-v1'). Worker runs the engine and feeds transitions via push (D-020). Separate train seed set from eval seed set (TEST_STRATEGY); eval is greedy (epsilon 0).",
      "M4.10 repeated-seed eval vs Fixed baseline: compare summaries across several eval seeds, record ACTUAL run numbers (do not hide failures/regressions, R2). This is the core M4 exit-criteria work.",
      "M4.7 training worker protocol (main<->worker messages, ARCHITECTURE boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
