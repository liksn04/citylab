{
  "project": "Neural City Lab",
  "version": "0.1.0",
  "activeMilestone": "M4",
  "activeMilestoneTitle": "Shared DQN Training",
  "milestoneStatus": {
    "M0": "done",
    "M1": "done",
    "M2": "done",
    "M3": "done",
    "M4": "active",
    "M5": "locked",
    "M6": "locked"
  },
  "gates": {
    "M0_appShell": true,
    "M0_designTokens": true,
    "M0_agentWorkflow": true,
    "M0_fixedPreview": true,
    "M1_seedReproducibility": true,
    "M1_routeCorrectness": true,
    "M1_queueCorrectness": true,
    "M1_metricsCorrectness": true,
    "M1_fixedBaselineStable": true,
    "M2_controllerInterface": true,
    "M2_minGreenContract": true,
    "M2_maxPressureDeterministic": true,
    "M2_scenarioResultsSaved": true,
    "M2_noSuperiorityAssumption": true,
    "M2_goldenBaselinePreserved": true,
    "M3_seedSetComparison": true,
    "M3_configHashPersisted": true,
    "M3_metricVersionPersisted": true,
    "M3_rawVsAggregateSeparated": true,
    "M3_reanalyzableExport": true
  },
  "lockedUntil": {
    "maxPressure": "M2",
    "experimentRunner": "M3",
    "tensorflowTraining": "M4",
    "dqn": "M4",
    "analyticsDashboard": "M5",
    "sumoIntegration": "post-MVP",
    "realMap": "post-MVP",
    "accounts": "post-MVP"
  },
  "lastSession": {
    "date": "2026-09-22",
    "summary": "M4.6 DQN update step (TensorFlow.js; single gradient step, NO training loop). Phase C: D-019 in docs/DECISIONS.md. src/rl/dqnUpdate.ts: DqnHyperparams + DEFAULT_DQN_HYPERPARAMS{gamma:0.95, learningRate:1e-3}; DqnTrainer(model, params?) owns an Adam optimizer; trainStep(batch) computes y = r + gamma*maxQ_target(next)*(1-done) (target net, no grad, tf.tidy) and Huber loss on the online Q of the taken action (one-hot mask), runs one optimizer.minimize step, returns finite loss, disposes all tensors; throws on empty batch; dispose() frees Adam accumulators. reward used raw (D-017, no scaling); batch size + target-sync cadence belong to the training loop (M4.7/M4.9). src/rl/dqnUpdate.test.ts (6): finite loss, done-only target, loss decreases over 150 steps on a fixed batch (learns), empty-batch throw, sane defaults, leak-0 (tf.memory().numTensors flat across build/10 steps/dispose). npm run check PASS (35 files, 244 tests, build ok). golden/m2/summary()/metricVersion unchanged; no engine wiring. Milestone/gates UNCHANGED (M4 active). Prior M4.1-M4.5 done.",
    "nextActions": [
      "M4.7 training worker protocol (src/workers/, ARCHITECTURE main<->worker): message schema (start/step/stop, progress+loss reports), a pure protocol function + message integration test; move the training batch path into a worker so rendering never blocks (R3). Assemble model/buffer/trainer inside the worker.",
      "M4.8 model save/load (tf.io; IndexedDB or serialization) -> load-after-save prediction parity test. M4.9 eval runner: wire DqnController into TrafficEngine (controllerKind 'dqn', greedy policy = argmax Q) + extend provenance/runConfig (D-010, controllerId 'dqn-v1'), train/eval seed separation (TEST_STRATEGY). M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "M4.7 training worker protocol (main<->worker messages, ARCHITECTURE boundary) -> M4.8 model save/load (prediction parity) -> M4.9 evaluation runner: wire DqnController into TrafficEngine (controllerKind 'dqn') + extend provenance/runConfig (D-010), train/eval seed separation (TEST_STRATEGY) -> M4.10 repeated-seed eval vs Fixed baseline; record failing scenarios too (R2).",
      "If observation proves too weak with pressure-only, bump D-015 alt (c): include raw per-axis queue load, which requires extending the ObservationInput engine seam (ADR + observation/model fixtures).",
      "ENV note (not a project change): npm install ERESOLVEs here (vitest@5 peerOptional @types/node ^22||>=24 vs root @types/node@20), dropping fake-indexeddb so dexieRunStore.test fails to load. Install with `npm install --legacy-peer-deps` to complete node_modules; no code/package.json change needed.",
      "M6.6 owns persistence migration tests across Dexie versions (v1->v2); no shipped data yet."
    ]
  }
}
