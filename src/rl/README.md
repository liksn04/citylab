# RL module lock

This directory is intentionally empty until **Milestone M4**.

Do not add DQN/replay-buffer/model code during M1–M3. The simulator, baseline controller, and experiment provenance must exist first.
